#include <stdio.h>
#include<iostream>
using namespace std;
int main()
{int a[10][10],i=0,j=0,r,c;
    printf("Enter the array size(R,C):\n");
    scanf("%d%d",&r,&c);
    printf("\nEnter no.s:");
    for(;i<r;++i)
    {for(;j<c;++j)
    cin>>a[i][j];
    }cout<<a[0][0];
    return 0;
}
