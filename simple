#include<iostream>
#include<vector>
using namespace std;
int main()
{vector<int> t;
int n,c=0,d=0;
    for (int i=0;cin>>n;++i)
    {if(n<=99||n>=0)
        t.push_back(n);
        
        if(n==42)
        {c++;d=i;}
    }
    if (c)
    for(int i=0;i<d;++i)
    {
        cout<<t[i]<<endl;
    }
    return 0;
    
}
